{
  "baseURI": "&{PLATFORM_LOGIN_URL|http://dfq-ui-login:8080}",
  "condition": "${find(request.uri.path, '^/enduser-login|^/platform-login')}",
  "handler": {
    "config": {
      "filters": [
        {
          "config": {
            "failureHandler": "DefaultRoute",
            "mappings": {
              "/enduser-login": "/",
              "/platform-login": "/"
            }
          },
          "type": "UriPathRewriteFilter"
        }
      ],
      "handler": "ReverseProxyHandler"
    },
    "type": "Chain"
  },
  "name": "login"
}
