{
  "baseURI": "&{PLATFORM_ADMIN_URL|http://dfq-ui-admin:8080}",
  "condition": "${find(request.uri.path, '^/platform-ui')}",
  "handler": {
    "config": {
      "filters": [
        {
          "config": {
            "failureHandler": "DefaultRoute",
            "mappings": {
              "/platform-ui": "/"
            }
          },
          "type": "UriPathRewriteFilter"
        }
      ],
      "handler": "ReverseProxyHandler"
    },
    "type": "Chain"
  },
  "name": "platform-ui"
}
